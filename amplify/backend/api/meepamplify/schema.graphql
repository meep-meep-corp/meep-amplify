type Vehicules @model {
  id: ID!
  name: String!
  description: String
  location: String
  status: String!
  battery: Int
}

type Providers @model {
  id: ID!
  name: String!
  description: String
}

type User @model {
  id: ID!
  name: String!
  email: String
}